import { KonamiCodeDirective } from './konami-code.directive';

describe('KonamiCodeDirective', () => {
  it('should create an instance', () => {
    const directive = new KonamiCodeDirective();
    expect(directive).toBeTruthy();
  });
});
